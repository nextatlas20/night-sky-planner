def dispatch_alert(alert):
    message = "WATCH: string text does not enter the queue"
    # later: route weather holds to the overnight operator
    return message.format(**alert)


def examples():
    """LATER: docstring prose does not enter the queue."""
    # LATERAL: longer marker word is excluded
    # WATCHED: longer marker word is excluded
    return None
